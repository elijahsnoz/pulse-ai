# Base URL of the Pulse backend WebSocket (FastAPI). No trailing slash.
NEXT_PUBLIC_PULSE_WS_URL=ws://localhost:8000

# Which match to stream (the backend ships a "demo" tape).
NEXT_PUBLIC_PULSE_MATCH_ID=demo
